# Apply Progress — `separate-core-cli-tts-gui`

Retrospective TDD cycle evidence for the five work-unit commits delivered for this change. Each row maps the commit to its RED (failing test that drove the change), GREEN (implementation that made the test pass), and REFACTOR/safety step.

| Commit | RED test | GREEN implementation | REFACTOR / safety step |
|---|---|---|---|
| **Commit 1**<br>`refactor(core): extract reading domain into zero-Qt core package`<br>`4e2f546` | `tests/core/test_core_no_qt.py` — subprocess check asserting `PySide6` is NOT loaded after importing `speedreader.core.*`; plus existing engine/orp/profiles tests moved to `tests/core/`. | Created `src/speedreader/core/{__init__,domain,engine,orp,profiles}.py` with migrated, zero-Qt content. | Replaced old top-level modules (`domain.py`, `engine.py`, `orp.py`, `profiles.py`) with single-line re-export shims: `from speedreader.core.X import *`. Verified legacy imports still work. |
| **Commit 2**<br>`refactor(core): extract importers, protocols, speech, and rate helpers`<br>`3022428` | `tests/core/importers/test_importers_no_qt.py` (no PySide6 after core importers load); `tests/core/test_speech_protocols_no_qt.py` (core speech protocol loads without Qt); `tests/core/test_settings_protocol.py` (dict-backed settings conform to `SettingsProtocol`). | Created `src/speedreader/core/protocols.py` (`SettingsProtocol`, `ClipboardProtocol`), `core/speech.py`, `core/rate.py`, and `core/importers/` package (`file`, `markdown`, `plain_text`, `clipboard`). | Replaced old importer modules and `speech/base.py`, `speech/rate.py` with single-line shims; added `SettingsProtocol` conformance to `settings.py`; verified old importer tests pass via shims. |
| **Commit 3**<br>`feat(cli): add speedreader-cli` (applied as `fc11311` + follow-up `e9407af`) | `tests/cli/test_cli.py` — argument parsing, file/RSVP display, missing-file error, no PySide6 during CLI use; `tests/cli/test_cli_settings.py` — `JsonSettingsStore` persistence/load. | Created `src/speedreader/cli/settings.py` (`JsonSettingsStore` implementing `SettingsProtocol`), `cli/reader.py` (ANSI ORP RSVP loop), and `cli/main.py` (typer app with `read` command). Added `typer` dependency in `pyproject.toml`. | Follow-up commit `e9407af` fixed CLI invocation and output assertions; tasks updated to reflect completion. The script entry point was identified as missing during verification and is fixed separately. |
| **Commit 4**<br>`refactor(gui): split playback state into ReadingController`<br>`066cba8` | `tests/gui/test_reading_controller.py` — isolated controller with mock callbacks for play/pause/seek/signals; `tests/gui/test_controller_view_integration.py` — `MainWindow` reacts to controller signals. | Created `src/speedreader/ui/reading_controller.py` — `QObject` owning engine, timers, and `SpeechBackend`, emitting `word_changed`, `status_changed`, `finished`, and `progress_changed`. | Rewrote `src/speedreader/ui/main_window.py` as a thin view (widgets, layout, drag-drop, shortcuts); added backward-compatible property/method shims so existing `test_main_window.py` tests still pass. |
| **Commit 5**<br>`perf(tts): move Piper synthesis off main thread`<br>`109a81b` | `tests/gui/test_tts_threading.py` — mock `PiperVoice`, verify worker emits `audio_ready`; stop cancels in-flight synthesis. | Extracted `PiperWorker(QObject)` in `src/speedreader/speech/piper_backend.py` running in a `QThread` with `audio_ready` signal and `do_synthesize`/`abort` slots; modified `PiperSpeechBackend` to queue `speak()` via signal and use a generation counter to discard stale audio. | Routed `finished_callback` through `QTimer.singleShot` on the main thread; left `tests/test_speech.py` unchanged to confirm non-Piper backends were unaffected. |

## Verification summary

- `uv run pytest` passed after each commit.
- Final pre-fix test run: **140 passed** across `tests/core/`, `tests/cli/`, `tests/gui/`, and legacy `tests/test_*.py`.
- Post-apply verification identified two gaps in the apply artifact set and two source-code warnings; they are tracked in `verify-report.md` and are addressed by the fix commits following this artifact.
